import 'package:flutter/material.dart';

class TicketCanvas extends StatelessWidget {
  const TicketCanvas({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
